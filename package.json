{
  "name": "score-fill-skill",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cf:dev": "wrangler pages dev dist --port 8788 --env-file .dev.vars",
    "cf:deploy": "wrangler pages deploy dist"
  },
  "dependencies": {
    "coze-coding-dev-sdk": "^0.7.24",
    "jszip": "^3.10.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
